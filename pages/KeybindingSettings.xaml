<Page x:Class="SchneidMaschine.pages.KeybindingSettings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Tastenbelegung" Width="Auto" Height="Auto">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titel -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Tastenbelegung konfigurieren" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Klicken Sie auf 'Ändern' um eine neue Taste zuzuweisen" FontSize="14" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- TabControl für verschiedene Modi -->
        <TabControl Grid.Row="1" FontSize="16">
            <!-- Einzel-Schritt -->
            <TabItem Header="Einzel-Schritt">
                <DataGrid x:Name="DataGridEinzelSchritt" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="LightGray" RowHeight="40">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Aktion" Binding="{Binding DisplayName}" IsReadOnly="True" Width="2*" FontSize="14"/>
                        <DataGridTextColumn Header="Taste" Binding="{Binding KeyName}" IsReadOnly="True" Width="*" FontSize="14">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="DarkBlue"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Neu zuweisen" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Ändern" Click="BtnChangeKey_Click" Tag="{Binding Action}"
                                            Width="100" Height="30" FontSize="12"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Halb-Automatik -->
            <TabItem Header="Halb-Automatik">
                <DataGrid x:Name="DataGridHalbAuto" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="LightGray" RowHeight="40">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Aktion" Binding="{Binding DisplayName}" IsReadOnly="True" Width="2*" FontSize="14"/>
                        <DataGridTextColumn Header="Taste" Binding="{Binding KeyName}" IsReadOnly="True" Width="*" FontSize="14">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="DarkBlue"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Neu zuweisen" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Ändern" Click="BtnChangeKey_Click" Tag="{Binding Action}"
                                            Width="100" Height="30" FontSize="12"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Automatik -->
            <TabItem Header="Automatik">
                <DataGrid x:Name="DataGridAuto" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="LightGray" RowHeight="40">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Aktion" Binding="{Binding DisplayName}" IsReadOnly="True" Width="2*" FontSize="14"/>
                        <DataGridTextColumn Header="Taste" Binding="{Binding KeyName}" IsReadOnly="True" Width="*" FontSize="14">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="DarkBlue"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Neu zuweisen" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Ändern" Click="BtnChangeKey_Click" Tag="{Binding Action}"
                                            Width="100" Height="30" FontSize="12"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Auf Standard zurücksetzen" Width="220" Height="40" FontSize="14" Margin="0,0,10,0" Click="BtnReset_Click"/>
            <Button Content="Speichern" Width="120" Height="40" FontSize="14" Margin="0,0,10,0" Click="BtnSave_Click"/>
            <Button Content="Schließen" Width="120" Height="40" FontSize="14" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Page>
